@inherits LayoutComponentBase

<MudThemeProvider />
<MudDialogProvider />
<MudSnackbarProvider />

<MudLayout>
    <MudAppBar Elevation="1">
        <MudIconButton Icon="@Icons.Material.Filled.Menu" Color="Color.Inherit" Edge="Edge.Start"
                       OnClick="@((e) => DrawerToggle())" />
        <MudText Typo="Typo.h6" Class="ml-3 title-spiritweb">SpiritWeb</MudText>
        <MudSpacer />
        @* pictogramme-button 3points *@
        @*<MudIconButton Icon="@Icons.Material.Filled.MoreVert" Color="Color.Inherit" Edge="Edge.End" />*@
    </MudAppBar>

    <MudDrawer @bind-Open="@_drawerOpen" Elevation="2" Class="bodyback">
        <MudDrawerHeader> 
            <MudText Typo="Typo.h6" Class="title-menu">Menu</MudText>
        </MudDrawerHeader>
        <NavMenu />
    </MudDrawer>
    <SpiritWeb.Client.Pages.PageTransitionLayout>   

        <MudMainContent>
            <MudContainer MaxWidth="MaxWidth.ExtraLarge" Class="my-4 pt-4">
                @Body
            </MudContainer>
        </MudMainContent>
    </SpiritWeb.Client.Pages.PageTransitionLayout>
    <Footer />
</MudLayout>


<style>

    html, body, .navmenu, .footer {
        font-family: "Cambria", Cambria, serif;
    }
    .bodyback{
        background-color: #4939d1; /* Couleur de fond */
        color: #fff; /* Couleur du texte */
    }

    .title-spiritweb, .title-menu {
        font-family: "Cambria", Cambria, serif;
    }
</style>